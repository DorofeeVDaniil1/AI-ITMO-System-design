services:
  face-gate-poc:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    command: >
      bash -c "pip install -q -r requirements.txt
      && uvicorn poc.app.main:app --host 0.0.0.0 --port 8000"

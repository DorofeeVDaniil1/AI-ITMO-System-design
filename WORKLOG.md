# Worklog

Таймбокс ~4 часа. План на старте: сначала решения (архитектура → ML → мониторинг → продукт/риски), потом тонкий PoC под fail-closed, в конце README и саморефлексия. Так и шло, без «сначала напишу сервис на 20 файлов».

Что съехало: чуть дольше обычного ушло на то, чтобы demo-матчинг не врал на e-1001 (fixture override вместо подгонки векторов). Не планировал это отдельно — поймал тестами. Первый push был одним большим коммитом; историю потом разбил на слои под скоуп, чтобы по git log было видно ход работы.

Сознательно вырезал: свою FR-модель, ANN в коде, UI охраны, железо турникета, k8s, разбор 152-ФЗ, гости/tailgating в MVP. Они либо запрещены ТЗ, либо раздувают репо без новых решений. PoC только policy + audit на пяти событиях.

Коммиты идут слоями: скелет → architecture → ml/monitoring → product/risks → poc → tests → meta. Это совпадает с тем, как реально двигался скоуп: сначала рамка и дизайн, код — в конце.
